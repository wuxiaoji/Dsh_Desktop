{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the local launcher window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
